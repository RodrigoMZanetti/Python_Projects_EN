meters = float(input("How many meters do you want to convert?: "))

centimeters = meters * 100
millimeters = meters * 1000

print(f"{meters} meters have {centimeters} centimeters and {millimeters} millimeters")